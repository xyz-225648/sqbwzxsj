# 更新日志

版本号规范：V主.次.补丁 —— **主版本**＝架构改动 / 重大功能，**次版本**＝新功能与界面优化，
**补丁号**＝Bug 修复与微小调整（不新增功能）。

## v2.3.2

### 修复（实机反馈三连）

- **Windows 一键更新卡在黑窗口、程序没换成**：替换脚本原来只等助手进程退出，但**主程序还占着 exe**，move 必然失败；而且用 start 会弹出黑窗口。现在：先用 vbs 以完全隐藏方式启动、先 taskkill 结束主程序、再重试 move 40 次，失败也会把新文件留下并记日志。
- **安卓下载到的包名/通知写着 2.2.2**：DownloadManager 的文件名与通知标题用的是**当前安装版本**，不是要下载的目标版本 —— 看着像下错了。现在按 program.txt 里的目标版本命名。
- **安卓切换作息后跳到浏览器、且要重进才生效**：页面用了 location.reload()，在基于远端 URL 注入的 WebView 里被当成外链丢给系统浏览器。现在优先调用原生 reloadPage()（页内重载）。

---

## v2.3.1

### 修复

- **桌面版「一键更新」下载必失败**：生成 program.txt 时资产名只把空格换成了 %20，没做百分号编码；中文文件名原样留在 URL 里，壳构造请求时会在 ASCII 编码处抛 UnicodeEncodeError（提示「位置 50-62 不在 0-127 范围内」）。现在三层都修了：① 生成 program.txt 时对资产名做 quote() 编码（根因）；② 页面拿到非 ASCII 地址时先 encodeURI 再交给壳（双保险）；③ 壳的 download_file 里加 ascii_safe_url()，老页面传裸中文地址也不炸。

---

## v2.3.0

### 下载

| 平台 | 文件 | 说明 |
|---|---|---|
| **Windows** | 宿迁职业技术学院作息时间表.exe | 双击即用，免安装（需 WebView2，Win11 自带） |
| **安卓手机** | 宿迁职业技术学院作息时间表.apk | 允许「安装未知来源应用」后安装 |
| **浏览器** | index.html | 任意浏览器直接打开 |

### 新增：按日期切换作息（周六半天课 / 周日晚自习 / 节假日调休）

- **周六**：全校统一上午 4 节课（8:00 起、11:30 午休），午休后到 **19:00 关寝**为休息；通识教育与其他学院同表
- **周日**：白天休息，晚自习（晚一/晚二/晚三）按周内时间，关寝沿用各学院
- **放假**：全天休息、19:00 关寝
- **调休/突发调整**：见下

### 校历信息改放仓库文件 calendar.txt（改良点）

特殊日表不再写死在页面里，而是读仓库里的 [calendar.txt](calendar.txt)：
每行一条、支持日期区间（如 `2026-01-20~2026-02-25 holiday 寒假`），**改它不用改页面、不用发版**；
读不到就退回「周末规则 + 手动覆盖」，不会白屏。

### 本机手动覆盖（页头新增「今天按」下拉）

学校临时停课、提前放假时，用户在页头直接选「周内 / 周六 / 周日 / 放假」，**立即生效、只影响本机**；
手动选择时顶部提示会写明「今日是<周几>，你选择了<哪套>时间 · <描述>，请以学校实际安排为准」。
设置面板里也有同一个下拉，两处联动。

### 其他修正

- 桌面版「一键更新」：更新弹窗与「关于」里的按钮直接走 本地壳 `/download`（下载 + 核对 sha256）→ `/apply`（退出后自我替换并重启）
- exe 不再把**更新**的页面降级：只在新版号更大时才替换（本地构建/镜像滞后时会误降级，已修）
- 连续休息段合并显示为一条（原来每个时段都列一行「休息」）
- 安静期提示：切换作息后页面会重载一次并按新值重新解析（当日缓存不会挡住切换）

---

## v2.2.2

### 下载

| 平台 | 文件 | 说明 |
|---|---|---|
| **Windows** | 宿迁职业技术学院作息时间表.exe | 双击即用，免安装（需 WebView2，Win11 自带） |
| **安卓手机** | 宿迁职业技术学院作息时间表.apk | 允许「安装未知来源应用」后安装 |
| **浏览器** | index.html | 任意浏览器直接打开 |

### 修复（issue IKI0KK）

- 【高】**apk 镜像四条线路全 404**（两个坑叠加）：① 发布 v2.2.1 时仓库文件名漏了 `v`，与 `program.txt` 路径不一致；
  ② 就算名字对了，jsDelivr 的 `@v2.2.1` 解析的是 **GitHub 上的 tag**，而 tag 建在文件提交**之前** ——
  tag 的树里永远没有这个文件，那条 URL 永久 404。现在文件名对齐 tag，线路改成 **tag 优先 + `@master` 兜底**
  （`?t=<apk sha 前 8 位>` 打散 CDN 缓存，页面仍核对 `apkSha256`）。实测 `@master` 与 ghproxy 均 200 / 127848 字节。
  发布流程改为「apk 先随 PR 提交 → 再建 tag」。
- 【中】**clone 下来直接跑不起来**：六个脚本硬编码本地页面名，仓库里只有 `index.html`。现在按
  「中文名 → `index.html`」自动找，实测模拟全新 clone 时 `check_page.py` 退出码 0。
- 【低】**闸门静默跳过**：`functest_page.py` 的两种"跑不了"情况原来都 `return 0`（= 假通过），
  现在都明确报错返回 2；浏览器候选表补 Linux / macOS 路径。
- 【低】**apk 签名钥匙**：`debug.keystore` → `release.keystore`，**沿用同一把钥匙**（换钥匙会让覆盖安装失败）。
  安卓 APK 本来就自签名，不需要 CA 证书（CA 只针对 Windows exe 的 SmartScreen）。
- 【低】**exe 版本信息按模板补齐**：文件说明 / 文件版本 2.2.2.0 / 产品名称 / 产品版本 2.2.2 /
  语言=中文(简体，中国)；公司名称与版权留空；版本号从页面 `APP_VERSION` 自动生成。
- 【提示】校园网 / 内网优先用应用内「一键下载安装包」（直连码云），浏览器走镜像线路并自动降级。

---

## v2.2.1

### 下载

| 平台 | 文件 | 说明 |
|---|---|---|
| **Windows** | 宿迁职业技术学院作息时间表.exe | 双击即用，免安装（需 WebView2，Win11 自带） |
| **安卓手机** | 宿迁职业技术学院作息时间表.apk | 允许「安装未知来源应用」后安装 |
| **浏览器** | index.html | 任意浏览器直接打开 |

### 修复：手机从 gitee 下到的 apk 会变成 `.apk.zip`

- **根因（实测）**：gitee 附件的 CDN 对 `.apk` 固定返回 `Content-Type: application/zip` ——
  同一个发行版里的 `.exe` 却是 `application/vnd.microsoft.portable-executable`，说明是 gitee 的
  MIME 映射问题。我建了临时发行版做对照：同一份文件分别用
  `application/vnd.android.package-archive` 和 `application/octet-stream` 上传，**返回的都是 zip**，
  上传端指定 Content-Type 没用（实验完已删除）。手机浏览器按 MIME 补后缀 →
  `宿迁职业技术学院作息时间表.apk.zip`。文件本身没坏：123752 字节，md5 与本地一致。
- **修法**：apk 同时作为**仓库文件**提交一份（`apk/sqzy-timetable-<tag>.apk`）。页面在**浏览器里**
  （没有原生桥）改走支持 CORS 的代理 CDN —— jsdelivr 三个域名 + ghproxy 依次回退（实测都带
  `access-control-allow-origin: *`）—— `fetch` 成 blob 后用 `a.download` **自己指定文件名**保存。
  文件名由页面说了算，与服务器 MIME 无关，所以浏览器也一定存成 `.apk`；拿到字节后先核对
  `program.txt` 里的 sha256，对不上就换下一条线路。全部不通才回退到 gitee 附件，并提示
  「若下成 `.apk.zip` 就把后缀改回 `.apk`」。
- **新增**：应用内「把安装包分享给同学」（微信 / QQ / 蓝牙 / 隔空投送类应用）。工程不带 AndroidX，
  所以自带了极简 `ApkProvider`（只暴露 `cacheDir/share`，`exported=false`，
  靠 `FLAG_GRANT_READ_URI_PERMISSION` 一次性授权），分享的是本机这份安装包。
- 应用内（有原生桥）仍然走系统 DownloadManager：显式文件名 + 正确 MIME，本来就不会被改名。

---

## v2.2.0

### 下载

| 平台 | 文件 | 说明 |
|---|---|---|
| **Windows** | 宿迁职业技术学院作息时间表.exe | 双击即用，免安装（需 WebView2，Win11 自带） |
| **安卓手机** | 宿迁职业技术学院作息时间表.apk | 允许「安装未知来源应用」后安装 |
| **浏览器** | index.html | 任意浏览器直接打开 |

### 新增与修复（来自 issue IKHWKA）

- 【高】安卓版「检查更新」必失败：gitee 的原始文件一律按 `text/plain` +
  `X-Content-Type-Options: nosniff` 返回，而且不带 CORS 头 —— `<script src=version.txt>` 会被
  Chromium 拒绝执行，`fetch` 又被跨域挡住，两条路都不通。现在按形态分三条路：桌面版走 exe 的本地
  接口 `/latest`；安卓版走原生桥 `fetchUrl`（HttpURLConnection 不看 MIME、不受 CORS 限制）；
  网页版 fetch GitHub 镜像（`raw.githubusercontent.com` 带 `Access-Control-Allow-Origin: *`）。
  三端都能正常检查更新，并且不再靠加载远程脚本。
- 【高】点通知打不开软件：安卓通知原来挂的是空 PendingIntent，点了没反应，现在补上
  `setContentIntent`；Windows 的 toast 补上 `activationType="protocol" launch="sqzy:open"`，
  并在启动时把 `sqzy:` 协议注册到 `HKCU\Software\Classes\sqzy`（只写当前用户，不用管理员）。
  点通知就拉起本程序；已经开着（哪怕藏在托盘里）时只把已有窗口叫到前台，不会再开第二个。
- 【中】不能一键后台下载安装：安卓改用 DownloadManager 后台下载（通知栏有进度，下完点通知安装）；
  Windows 新增「一键更新」——页面把 `program.txt` 里的直链和 sha256 交给 exe 的 `/download`，
  下载完先算 sha256，对不上就拒绝替换；再由 `/apply` 写一个替换脚本，等进程退出后用 `move`
  覆盖自身并自动重启（PyInstaller onefile 运行中不能覆盖自己）。课表内容和设置都不受影响。
  这两个接口都要令牌，只有本应用页面能调用。
- 【中】APK 不后台常驻：新增前台服务 `KeepAliveService`（`IMPORTANCE_MIN`，状态栏一条通知）
  和 `BootReceiver`（开机自启）；设置面板新增「后台运行（仅安卓手机版）」：后台常驻开关、
  一键申请「忽略电池优化」、打开应用设置，并按小米 / 华为·荣耀 / OPPO·一加 / vivo 分别写清
  自启动放行路径 —— 国产系统只关电池优化仍会被清后台。
- 【低】`program.txt` 增加 exe/apk 直链、sha256 和字节数（新增 `make_program_txt.py`，
  直接从本地发行包算出来），页面据此显示「一键更新」并做完整性校验。
- 【发布流程，未做成】原计划把 GitHub 镜像同步从 6 小时缩短到 30 分钟（网页版检查更新要走镜像），
  但一改就踩到 GitHub 的硬限制：**Actions 自带的 `GITHUB_TOKEN` 没有 `workflow` 权限**，
  被推的历史里只要有提交动过 `.github/workflows/**`，GitHub 就整体拒收，双向同步当场停摆
  （`refusing to allow a GitHub App to create or update workflow ... without workflows permission`）。
  已用 PR #16 把该文件恢复成与 GitHub 逐字节一致的版本，同步恢复（工作流 run 已 success）。
  **频率维持 6 小时**；这条坑写进了 README 的「GitHub 镜像」一节。要真改这个文件，
  得用带 `workflow` 权限的令牌分别推两边。

---

## v2.1.3

### 下载

| 平台 | 文件 | 说明 |
|---|---|---|
| **Windows** | 宿迁职业技术学院作息时间表.exe | 双击即用，免安装（需 WebView2，Win11 自带） |
| **安卓手机** | 宿迁职业技术学院作息时间表.apk | 允许「安装未知来源应用」后安装 |
| **浏览器** | index.html | 任意浏览器直接打开 |

### 修复（来自复查报告）

- 【中】桌面版「快速重启会落到别的端口」：`allow_reuse_address` 原来写成了在服务器**构造之后**赋值，
  而绑定发生在构造函数里，等于没生效；助手退出后 60 秒内 TIME_WAIT 没散就 bind 失败、退到 51901+，
  而 localStorage 按 `host:端口` 隔离，设置看起来又丢了。现在改成类属性（构造前就生效），
  实测同一端口退出后立刻重启可正常绑定。
- 【中低】本地接口继续收紧：去掉 `Access-Control-Allow-Origin: *`（页面与接口本来就同源，不需要它），
  并对带 `Origin` 的跨站请求一律 403（写接口的令牌校验见 v2.1.2）。
- 【低】首页分支加异常兜底：内置页面读不出来时不再静默断开连接，改为回一页说明并写 `api.log`。
- 【低】修掉 WebView2 检测里注册表路径的三处双反斜杠笔误。
- 【低】自检脚本：`/notify` 的断言按平台判断（非 Windows 上发不出系统通知是正常的），
  端口改为动态挑选，不再被上次残留进程 / TIME_WAIT 阻塞重跑。
- 【发布流程】`RELEASE_TAG` 的注释与实现对齐（跟着版本走、不回填旧 tag），并把发布顺序写进 README：
  **合并页面 PR → 立刻建同名 tag / 发行版 → 上传附件**，避免「tag 里的源码与附件不同版」
  和「下载链接指向还不存在的 tag」这两个窗口期问题。

---

## v2.1.2

### 下载

| 平台 | 文件 | 说明 |
|---|---|---|
| **Windows** | 宿迁职业技术学院作息时间表.exe | 双击即用，免安装（需 WebView2，Win11 自带） |
| **安卓手机** | 宿迁职业技术学院作息时间表.apk | 允许「安装未知来源应用」后安装 |
| **浏览器** | index.html | 任意浏览器直接打开 |

### 修复（复查 issue #IKHTOU）

- **本地接口不再谁都能调（CSRF）**：接口监听在 127.0.0.1，浏览器里打开的**任意网页**都能向它发 GET
  （CORS 只拦读响应、不拦发请求），于是可以伪造系统通知、改窗口置顶/关闭方式、打开外链、甚至把助手关掉。
  现在壳每次启动随机生成一个令牌，只注入给本程序自己的页面；`/notify` / `/state`（写）/ `/open` / `/quit` / `/config`
  必须带令牌，否则 403。页面本体、/ping、/version、/latest 这些只读接口保持开放。
- **「改完设置马上重启」不再丢设置**：以前设置只写在 WebView2 的 localStorage 里，刷盘时机不受程序控制，
  重启太快就可能回到默认。现在设置同时交给壳落一份 `settings.json`，启动时两份比时间戳、谁新用谁。
- **发版不再回填旧 tag**：以前为了让老页面里的下载链接也拿到最新安装包，把旧发行版的附件换成了新二进制，
  结果 `v1.0` 里下到的其实是 v2.1.1，tag 和二进制对不上。以后每个 tag 只放它自己那一版的安装包；
  老页面靠页面内的「程序本体有新版」提示引导到最新发行版。
  历史遗留：v1.0 / v2.0.1 / v2.1.0 的附件仍是回填过的二进制（已在各发行版说明里标注）。

---

## v2.1.1

### 下载

| 平台 | 文件 | 说明 |
|---|---|---|
| **Windows** | 宿迁职业技术学院作息时间表.exe | 双击即用，免安装（需 WebView2，Win11 自带） |
| **安卓手机** | 宿迁职业技术学院作息时间表.apk | 允许「安装未知来源应用」后安装 |
| **浏览器** | index.html | 任意浏览器直接打开 |

### 修复

- **自动更新以前只管网页，不管程序本体**（issue #IKHS2W）：网页能热更新，但装着的 exe / apk 旧了没人知道，于是会出现「课表是新的、程序还是半年前的」。现在三端都会比一次程序本体版本：
  - 桌面版：壳通过新接口 `/version` 自报版本；页面从仓库的 `program.txt` 读当前发布的程序版本
  - 手机版：页面通过原生桥拿到安装包的 versionName
  - 落后时在「设置 → 关于」写明「程序本体 v2.1.0 → 有新版 v2.1.1」，并弹**一次性**提示，附「去下载新版」按钮
  - 已经是最新就只报平安；老版程序（不会自报版本）或取不到 `program.txt` 时静默跳过，不乱提示
- **手机版**：点下载链接不再把当前页面顶掉（外链改为交给系统浏览器打开）。
- **发布流程**：publish.sh 多生成一个 `program.txt`（记录当前发布的 exe/apk 版本）。
- **测试卫生**：外壳测试的自启动项复原挪进了 `finally` —— 之前流程中途出错会在机器上真的留下一条开机自启（这次就留了一条，已清理并加了保护）。

---

## v2.1.0

### 下载

| 平台 | 文件 | 说明 |
|---|---|---|
| **Windows** | 宿迁职业技术学院作息时间表.exe | 双击即用，免安装（需 WebView2，Win11 自带） |
| **安卓手机** | 宿迁职业技术学院作息时间表.apk | 允许「安装未知来源应用」后安装 |
| **浏览器** | index.html | 任意浏览器直接打开 |

### 新增

- **Windows 桌面版支持开机自启动**（issue #IKHP3W）：设置面板新增「开机自启动」开关，
  勾上以后每次登录 Windows 都会自动在**托盘**里跑起来（不弹窗口），课表提醒照常发。
  只写当前用户的启动项（HKCU\Software\Microsoft\Windows\CurrentVersion\Run），
  不需要管理员权限，取消勾选即刻移除；关掉软件不会留下别的东西。
- 启动项路径会**自愈**：以后把 exe 挪了目录、或换成新版本，只要启动过一次，
  启动项里的路径就会被刷新成当前的，不会出现「开机启动失效」还找不到原因。
- 开机自启动拉起时如果发现已经有一个在跑（用户自己先开过），这次会**安静退出**，
  不会在开机时把窗口弹到人脸上。

### 修复

- 无。

---

## v2.0.1

### 下载

| 平台 | 文件 | 说明 |
|---|---|---|
| **Windows** | 宿迁职业技术学院作息时间表.exe | 双击即用，免安装（需 WebView2，Win11 自带） |
| **安卓手机** | 宿迁职业技术学院作息时间表.apk | 允许「安装未知来源应用」后安装 |
| **浏览器** | index.html | 任意浏览器直接打开 |

### 修复

- **桌面版**：助手进程没起来时，窗口会开出一页「无法访问此页面」。现在先探一下本地接口，
  探不通就直接把页面交给窗口（功能照旧，只是系统通知与「置顶」不可用）。
- **桌面版**：启动后在后台查更新，若新版本回来得太快、窗口还没建好，热替换会静默失败，
  要等下次打开才生效；现在会等窗口就绪再替换。
- **桌面版**：选「关闭＝直接退出」后，退出时会把托盘图标一并收掉，不再留下一个点不动的幽灵图标。
- **桌面版**：「检查更新」接口现在和程序自身的更新检查一致，master / main 两个分支都会尝试。
- **桌面版**：下载下来的页面会按文本模式写盘，在 Windows 上被自动改成 CRLF 换行，跟仓库里发布的那一份
  差出一千多个字节；现在按字节原样落盘，本地缓存与线上页面校验值完全一致。
- **手机版**：首次启动加载内置页面与热替换后的页面不是同一个存储来源，刚改的设置会在热替换时丢失；
  现在首次启动也走同一套加载方式，设置自始至终是同一份。
- **手机版**：热替换后按返回键会退回替换前的旧界面；现在页面上会清掉这条历史。
- **页面**：发系统通知不再「发出请求就报成功」。本地通知通道没响应时会如实提示，
  不再显示「已发给系统通知中心」这种假消息。
- **页面**：设置面板里几处「元素必然存在」的假设加上兜底，缺元素也不会让整个面板初始化中断。