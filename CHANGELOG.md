# 更新日志

版本号规范：V主.次.补丁 —— **主版本**＝架构改动 / 重大功能，**次版本**＝新功能与界面优化，
**补丁号**＝Bug 修复与微小调整（不新增功能）。

---

## v2.0.1

### 下载

| 平台 | 文件 | 说明 |
|---|---|---|
| **Windows** | 宿迁职业技术学院作息时间表.exe | 双击即用，免安装（需 WebView2，Win11 自带） |
| **安卓手机** | 宿迁职业技术学院作息时间表.apk | 允许「安装未知来源应用」后安装 |
| **浏览器** | index.html | 任意浏览器直接打开 |

### 修复

- **桌面版**：助手进程没起来时，窗口会开出一页「无法访问此页面」。现在先探一下本地接口，
  探不通就直接把页面交给窗口（功能照旧，只是系统通知与「置顶」不可用）。
- **桌面版**：启动后在后台查更新，若新版本回来得太快、窗口还没建好，热替换会静默失败，
  要等下次打开才生效；现在会等窗口就绪再替换。
- **桌面版**：选「关闭＝直接退出」后，退出时会把托盘图标一并收掉，不再留下一个点不动的幽灵图标。
- **桌面版**：「检查更新」接口现在和程序自身的更新检查一致，master / main 两个分支都会尝试。
- **手机版**：首次启动加载内置页面与热替换后的页面不是同一个存储来源，刚改的设置会在热替换时丢失；
  现在首次启动也走同一套加载方式，设置自始至终是同一份。
- **手机版**：热替换后按返回键会退回替换前的旧界面；现在页面上会清掉这条历史。
- **页面**：发系统通知不再「发出请求就报成功」。本地通知通道没响应时会如实提示，
  不再显示「已发给系统通知中心」这种假消息。
- **页面**：设置面板里几处「元素必然存在」的假设加上兜底，缺元素也不会让整个面板初始化中断。

### 清理

- 去掉页面与桌面版之间早已无人校验的访问令牌（SQZY_TOKEN）和一个从未被读取的配置项。
- 版本号改用 **V主.次.补丁** 规范，本版 **v2.0.1**；安卓 versionCode / versionName 同步为 20001 / 2.0.1。

### 测试

- 新增 **交互遍历** 测试（无头浏览器 + CDP，59 项）：悬停预览轴、钉住 / 取消、学院名跳转、
  回到顶部、设置面板与滚动锁、设置持久化、通知判定（只勾选部分学院时不许发别的学院 /
  整分钟被跳过会补发 / 跨天清掉旧记录 / 同一分钟不重复）、更新弹窗、缩放记忆、
  防右键·防调试·禁复制、手机版竖排点按。
- 新增 **桌面外壳** 端到端测试（17 项）：真的开一个窗口，验单实例、关闭到托盘、置顶
  （网页 → 助手 → 状态文件 → 窗口）、二次启动唤回前台、直接退出、助手进程收尾。
- 两个测试都已并入 publish.sh：发布前不过闸门就中止发布。

### 新增（自 v1.0 起累积）

- **桌面版**：窗口置顶（页头按钮 / Ctrl + T）、关闭到托盘、单实例（重复双击只唤回原窗口）、
  Windows 系统通知（带校徽图标）、设置面板中的「窗口」分区。
- **桌面版**：设置持久化修好了 —— 以前「改完不保存、下次打开又回到默认」。
- **手机版**：安卓系统通知（首次启动申请权限）、设置 / 关于面板、版本号显示。
- **三端**：设置面板（按学院开关通知、下课前 N 分钟、上课前 / 开始上课 / 下课时提醒、测试通知）、
  弹层滚动锁（面板里滑动不再带动后面的时间表）、Ctrl + 滚轮缩放并记住、深色适配与自适应横竖版。

---

## v1.0

首个发布版本：四学院一日作息时间轴，横版 / 竖版自动切换，当前时段高亮与倒计时，
点按时间轴钉住查看，三端（Windows / 安卓 / 浏览器）通用，完全离线，带自动更新。