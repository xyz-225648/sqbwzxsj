# 自动更新使用说明

## 原理（一句话）

软件启动时先秒开本地版本，然后在后台悄悄去码云问一句「有没有新版本」，
有就下载并静默替换，用户完全无感；断网时照旧使用，不影响任何功能。

只有改课表内容才需要发布；软件外壳（exe / apk）装了就不用再换。

## 一次性准备（约 5 分钟）

1. 注册并登录 码云 Gitee：https://gitee.com
2. 新建仓库，例如叫 sqzy-timetable，必须选「公开」
   （私有仓库的 raw 地址需要登录才能取，程序取不到）
3. 仓库建好后，先上传 index.html 和 version.txt 两个文件
   （用下面的发布脚本生成）
4. 你的 raw 地址就是：
   https://gitee.com/你的用户名/sqzy-timetable/raw/master/
   注意结尾那个斜杠不能少

## 以后每次改完课表

    python publish.py        （或 bash publish.sh，它只是转发）

发布脚本会依次跑完五道自检（逻辑断言 / 页面语法 / 渲染冒烟 / 交互遍历 / 本地接口），
全部通过才生成 release 目录；然后把里面的 index.html、version.txt 传到码云仓库（覆盖旧文件），
**并且把 index.html 也传成发行版附件**：

    python gitee_repo.py release upload <tag> release/index.html:index.html

为什么还要传附件：码云 raw 对**大文件**返回 451（The content may contain violation information，
实测 ≤24 KB 正常、≥39 KB 全被挡），页面有 148 KB —— 程序取新页面时会依次试
「发行版附件 → contents API → raw」，附件是第一条也是最稳的一条。

## 版本号怎么写

两个版本号**分开算、各管各的**：

| 版本号 | 管什么 | 什么时候升 | 写在哪里 |
|---|---|---|---|
| **页面版本**（热更新） | 网页本体 / 课表数据 | 改 `index.html` 就升 | 页面 `APP_VERSION`；发布时自动写进 `version.txt` |
| **程序本体版本**（重下安装包） | exe / apk 的壳 | **只有重打安装包才升** | `app.py` 的 `SHELL_VERSION`（exe）；`.apkbuild/app/AndroidManifest.xml` 的 `versionName` / `versionCode`（apk） |

- 纯页面 / 数据变更：只升**页面版本**，`program.txt` 原样不动 → 用户只会热更新，**不会**被提示重下安装包。
- 壳代码变更：升 `SHELL_VERSION` 和 `versionName` / `versionCode`，重打 exe / apk，
  `make_program_txt.py` 会把新的程序本体版本、直链、sha256 写进 `program.txt` → 用户才会看到「程序本体有新版」。
- 两者可以相同，也可以不同（页面比壳新、壳比页面新都允许）。

段位含义不变：主版本＝架构改动或重大功能，次版本＝新增功能与界面优化，补丁号＝修 Bug 与微调。

## 只改页面 / 数据（不换安装包）

1. 改页面 `APP_VERSION`（按上面的段位升），跑 `python publish.py`
2. 把 `release/index.html`、`release/version.txt` 覆盖到仓库根目录
3. 建同名 tag 的发行版，把 `index.html` 传成发行版附件（页面热更新的第一来源）
4. `release/program.txt` 和 exe / apk **不用动** —— 用户不会收到「程序本体有新版」

## 发新版（要换 exe / apk 时）

1. 升 `app.py` 的 `SHELL_VERSION` 和 `.apkbuild/app/AndroidManifest.xml` 的
   `versionName` / `versionCode`（两者必须一致）；页面 `APP_VERSION` 如需同步升也可以
2. 重新打包：exe 用 PyInstaller，apk 用 `.apkbuild/build_apk.py`
3. 跑 `python publish.py`，把 `index.html`、`version.txt`、`program.txt` 覆盖到仓库根目录
4. 按 `program.txt` 里的 tag（= 程序本体版本）建发行版，把 `index.html`、exe / apk **都传到这个 tag 下**：
   附件名保持不变（index.html 用于热更新，缺了会退回 contents API）。`program.txt` 里的直链和 sha256
   就是按「tag + 附件名」写死的，所以顺序不能颠倒：先合并页面 PR，立刻建 tag、传附件。

## 首次启用自动更新（只做一次）

    bash set-update-url.sh https://gitee.com/你的用户名/sqzy-timetable/raw/master/

这条命令会把地址写进 exe 和 apk 的源码里并重新打包。之后分发出去的就是
带自动更新能力的版本。

想临时关闭：  bash set-update-url.sh ""

## 各平台的更新表现

| 平台 | 表现 |
|---|---|
| Windows exe | 启动秒开，后台检查，有新版就悄悄换成新页面；**程序本体**有新版时「设置 → 关于」给出
「一键更新」：自己下载 → 校验 sha256 → 退出后替换自身并重启 |
| Android apk | 同上（打开后约 1~2 秒自动刷新成最新课表）；本体更新走 DownloadManager 后台下载，
下完点通知安装，覆盖安装不丢数据 |
| 网页版（放在服务器上） | 直接替换服务器上的 index.html，刷新即最新 |
| 手机浏览器打开的本地 html | 无法自动更新（浏览器跨域限制），需重新传文件 |

## 注意

- 码云 raw 是 302 跳转到 CDN 的，程序已按跟随重定向实现（实测通过）
- **码云 raw 挡大文件**：>约 25 KB 的文件匿名读会直接 451（同样内容换成小文件就正常），
  所以页面本体按「发行版附件 → contents API → raw」取；version.txt / program.txt 很小，raw 照常可用
- 更新只在启动时检查一次；已经开着的窗口不会自动变
- 页面检查更新不再用 `<script src>`：gitee raw 是 `text/plain` + `nosniff` 且没有 CORS 头，
  这条老路在浏览器里必然失败。现在桌面版走本地接口、安卓版走原生桥、网页版走 GitHub 镜像
  （`raw.githubusercontent.com` 带 CORS 头），所以镜像同步频率提到了 30 分钟一次
- Windows 的「一键更新」只有在**打包成 exe** 后可用；源码模式会明确拒绝自我替换
- 点系统通知＝回到课表：Windows 走 `sqzy:` 协议（注册在 `HKCU\Software\Classes\sqzy`），
  安卓走通知的 `setContentIntent`
- 版本号用「内容哈希 + 时间」生成，内容没变就不会触发下载
- 下载失败 / 地址写错 / 断网，一律静默忽略，继续用本地版本，不会白屏
